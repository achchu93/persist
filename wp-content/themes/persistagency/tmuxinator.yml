# run: tmuxinator start -p tmuxinator.yml
name: persist-theme

startup_window: editor 

windows:
  - watchers:
      - docker-compose up -d
      - nvm use 14
      - composer install
      - yarn
      - yarn start
  - editor: nvim
