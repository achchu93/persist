# run: tmuxinator start -p tmuxinator.yml
name: persist-theme

startup_window: editor 

windows:
  - watchers:
      - nvm use 14
      - composer install
      - yarn
      - yarn start
  - editor: nvim
