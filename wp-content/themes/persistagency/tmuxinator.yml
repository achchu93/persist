# run: tmuxinator start -p tmuxinator.yml
name: persist-theme

pre_window: docker-compose up -d

windows:
  - watchers:
      - nvm use 14
      - composer install
      - yarn
      - yarn start
  - editor: nvim

